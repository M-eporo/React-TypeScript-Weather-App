import styles from '../styles/logo.module.css';

const Logo = () => {
    return (
      <div>
        <h1 className={styles.title}>React + TypeScript Weather App</h1>
      </div>
    );
};

export default Logo;